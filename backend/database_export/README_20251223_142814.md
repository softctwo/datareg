# 数据库导出文件说明

**导出时间**: 2025-12-23 14:28:14

## 文件说明

1. **01_schema_*.sql** - 数据库结构脚本（DDL）
   - 包含所有表的创建语句
   - 包含索引、约束等

2. **02_data_*.sql** - 数据库数据脚本（DML）
   - 包含所有表的 INSERT 语句
   - 包含所有初始化数据

3. **init_database_*.sql** - 完整初始化脚本
   - 包含结构+数据的完整脚本
   - 可以直接用于初始化新数据库

## 使用方法

### 方法一：使用完整脚本（推荐）

```bash
# 1. 创建数据库
psql -U postgres -h localhost -d postgres -c "CREATE DATABASE datareg;"

# 2. 执行初始化脚本
psql -U postgres -h localhost -d datareg -f init_database_20251223_142814.sql
```

### 方法二：分步执行

```bash
# 1. 创建数据库
psql -U postgres -h localhost -d postgres -c "CREATE DATABASE datareg;"

# 2. 执行结构脚本
psql -U postgres -h localhost -d datareg -f 01_schema_20251223_142814.sql

# 3. 执行数据脚本
psql -U postgres -h localhost -d datareg -f 02_data_20251223_142814.sql
```

## 数据库信息

- **数据库名**: datareg
- **服务器**: localhost
- **用户**: postgres

## 注意事项

1. 执行脚本前请确保 PostgreSQL 服务正在运行
2. 如果数据库已存在，请先备份或删除
3. 执行脚本时请确保有足够的权限
4. 建议在生产环境使用前先在测试环境验证

